{{>sidebar}}
{{>header}}
{{>token}}

<div class="wrapper">
    <a href="/items/create" class="link create-item-button">Create Item</a>

    <button onclick="deleteSelectedItems()" type="submit">Delete</button>
    <div class="items table">
        <table>
            <thead>
                <tr>
                    <th>
                        <label class="checkbox">
                            <input  type="checkbox" onchange="checkAllBoxes(this)"> 
                        </label>
                    </th>
                    <th>Picture</th>
                    <th>Name</th>
                    <th>Price</th>
                    <th>Description</th>
                </tr>
            </thead>
            <tbody>
                {{#each items}}
                    <tr>
                        <td>
                            <label class="checkbox">
                                <input  type="checkbox" class="item-checkbox box-checkbox" data-id="{{_id}}"> 
                            </label>
                        </td>
                        <td><img src="/uploads/resized/{{picture}}" width="60px" alt=""></td>
                        <td><a class="link" href="/items/edit/{{_id}}">{{name}} {{personal_information.last_name}}</a></td>
                        <td>{{price}}</td>
                        <td>{{description}}</td>
                    </tr>
                {{/each}}
            </tbody>
        </table>
        <div class="pagination">
            {{#if (gt currentPage 1)}}
            <a href="?page={{subtract currentPage 1}}">Previous</a>
            {{/if}}
            {{#if (lt currentPage totalPages)}}
            <a href="?page={{add currentPage 1}}">Next</a>
            {{/if}}
        </div>
    </div>
</div>